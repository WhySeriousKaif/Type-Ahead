spring.application.name=SearchIQ

# MongoDB Atlas connection
spring.data.mongodb.uri=mongodb+srv://kaif00786001_db_user:8aZ7wzk7K8Fp9NT5@cluster0.ptejabv.mongodb.net/typeahead?retryWrites=true&w=majority

# Server port
server.port=${PORT:3002}

# CORS — allow the React frontend
searchiq.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:5175

# Batch Writer config
searchiq.batch.flush-interval-ms=${BATCH_FLUSH_INTERVAL_MS:30000}
searchiq.batch.flush-size=${BATCH_FLUSH_SIZE:100}

# Cache TTL in milliseconds (default 5 minutes)
searchiq.cache.ttl-ms=${CACHE_TTL_MS:300000}

# Logging
logging.level.com.searchiq=DEBUG
